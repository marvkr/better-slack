// Smart typography utilities
export function formatSmartQuotes(text: string): string {
  return text
}

export function formatDashes(text: string): string {
  return text
}

export function applySmartTypography(text: string): string {
  return formatDashes(formatSmartQuotes(text))
}
